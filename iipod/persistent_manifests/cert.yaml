---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: wildcard
  namespace: ${namespace}
spec:
  secretName: wildcard-tls
  dnsNames:
    - "${user_domain}"
    - "*.${user_domain}"
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
    group: cert-manager.io
